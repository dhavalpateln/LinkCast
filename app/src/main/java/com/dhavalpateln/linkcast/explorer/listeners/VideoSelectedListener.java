package com.dhavalpateln.linkcast.explorer.listeners;

import com.dhavalpateln.linkcast.database.VideoURLData;

public interface VideoSelectedListener {
    void onVideoSelected(VideoURLData videoURLData);
}
